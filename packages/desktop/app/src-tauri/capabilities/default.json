{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: no IPC permissions; the harness UI talks to the backend over the loopback HTTP/WebSocket wire, not through Tauri.",
  "windows": ["main"],
  "permissions": []
}
