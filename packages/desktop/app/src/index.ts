/**
 * The desktop shell's TypeScript face is intentionally empty: the shell is a
 * Tauri (Rust) application whose runtime lives in `src-tauri/`. This module
 * exists so the package participates in the standard workspace build.
 */
export {}
